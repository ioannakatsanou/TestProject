services:
  db:
    image: postgres:16
    container_name: agfb_postgres
    environment:
      POSTGRES_USER: agfb
      POSTGRES_PASSWORD: agfb
      POSTGRES_DB: agfb
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
